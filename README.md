# Shopkarr
A E-commerce project
## Development
To edit the styling, download the Tailwind v4 Standalone CLI for your OS, place it in the root directory as `tailwindcss`, and run:
`./tailwindcss -i ./static/src/input.css -o ./static/css/output.css --watch`